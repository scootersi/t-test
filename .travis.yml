jobs:
  include:
    - stage: Test
      language: python
      python: 3.9
      name: Unit Test Job 
      before_install:
        - python3 --version
        - pip3 install -U pip
        - pip3 install -U pytest
      script:
        - pytest
        - python hello.py 
    
    - stage: Run
    - if: branch = "dev"
      env: 
        STAGE=dev
      language: node_js
      node_js: stable 
      before_install:
        - npm install gulp-cli 
      env:
        - STAGE=dev
      script:
        - gulp --version
    - if: branch = "staging"
      env: 
        STAGE=staging
      language: node_js
      node_js: stable 
      before_install:
        - npm install gulp-cli 
      env:
        - STAGE=dev
      script:
        - gulp --version
         
    

      
